apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: home-finance
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: home-finance
  podMetricsEndpoints:
    - port: metrics
