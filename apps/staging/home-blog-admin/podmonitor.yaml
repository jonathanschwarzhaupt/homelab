apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: home-blog
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: home-blog
  podMetricsEndpoints:
    - port: metrics
