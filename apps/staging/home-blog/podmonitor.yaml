apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: home-blog
  # this is required so the pod monitor is picked up
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: home-blog
  podMetricsEndpoints:
    - port: metrics
