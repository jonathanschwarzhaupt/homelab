apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: linkding
spec:
  defaultBackend:
    service:
      name: linkding
      port:
        number: 9090
  ingressClassName: tailscale
  tls:
    - hosts:
        - linkding
